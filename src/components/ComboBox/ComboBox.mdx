import { Meta } from '@storybook/addon-docs'

<Meta title="COMPONENTS/ComboBox" />

# ComboBox

사용자가 목록에서 항목을 선택하거나 검색할 수 있는 드롭다운 메뉴

## Example

```jsx
import ComboBox from './ventileco-ui

function Example() {
  return (
    <div className="w-[240px]">
      <ComboBox value={} setValue={} list={}>
        <ComboBox.Label>ComboBox</ComboBox.Label>

        <ComboBox.Trigger>
          <button className="flex w-full items-center rounded-md border px-3 py-2">
            <ComboBox.Input placeholder="Fruits" />
            <div className="flex gap-2">
              <ComboBox.ClearButton></ComboBox.ClearButton>
              <ComboBox.ArrowButton></ComboBox.ArrowButton>
            </div>
          </button>
        </ComboBox.Trigger>

        <ComboBox.List>
          {({ optionList }) =>
            optionList.map((item, idx) => (
              <ComboBox.Item key={item.value} idx={idx} item={item}>
                <button className={`w-full px-3 py-2`}>{item.label}</button>
              </ComboBox.Item>
            ))
          }
        </ComboBox.List>
      </ComboBox>
    </div>
  )
}
```
